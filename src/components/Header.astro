<header class="main-header">
  <div class="logo-container">
    <img src="/images/logo.png" alt="Dream Breakfast Club logo" class="logo-image">
  </div>

  <nav class="main-nav">
    <ul>
      <li><a href="#today">Today's breakfast</a></li>
      <li><a href="#past">Past breakfasts</a></li>
      <li><a href="#about">About us</a></li>
    </ul>
  </nav>
</header>

<style>
  .main-header {
    background-color: #ffffffff; 
    padding: 0.5rem var(--spacing-md);
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 10;
    box-shadow: none; 
    text-align: center;
     z-index: 11; 
    
    padding-bottom: calc(0.5rem); 
  }

  .logo-container, .main-nav {
    position: relative;
    z-index: 12;
  }

  .logo-container {
    padding: 0.5rem 0;
  }
  
  .logo-image {
    max-width: 250px;
    height: auto;
  }
  
  .main-nav ul {
    display: flex;
    justify-content: space-around;
    padding: 0.5rem 0;
    font-family: var(--font-body); /* Aleo  */
    font-size: 1rem;
    white-space: nowrap;
    overflow-x: auto;
  }
  
  .main-nav a {
    padding: 0.3rem 0.5rem;
    color: black; 
    font-weight: 700;
    position: relative;
  }

  .main-nav a:hover {
  color: #C7B3F1;
  transition: color 0.1s ease;
}

  .main-header::after {  
    position: absolute;  
    bottom: -50px;
    left: 0px;  
    width: 100%;  
    content: " "; 
    background:radial-gradient(circle at 50% 0%, #ffffffff 25%, #C4DCD100 26%, #c4dcd100 40%);  
    background-size: 50px 100px;  
    height: 50px;
    background-repeat: repeat-x;  
    z-index: 10;
  }

  @media (min-width: 768px) {
    .main-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem var(--spacing-lg);
      padding-bottom: calc(1rem);
    }
    .logo-container {
      padding: 0;
    }
    .main-nav ul {
      justify-content: flex-end;
      gap: var(--spacing-lg);
      padding: 0;
      font-size: 1.1rem;
      overflow-x: visible;
    }
    .logo-image {
      max-width: 300px;
    }
  }
</style>